# Rust review skill

When reviewing Rust code, check ownership, error propagation, cancellation or timeout behavior, input bounds, public visibility, and test coverage. Prefer compiler-enforced invariants over comments.
